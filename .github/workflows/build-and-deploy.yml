name: Build and Deploy to Telegram

on:
  push:
    # Uncomment the line below to build on every commit to main branch
    # branches: [ main ]
    # Build on tags (e.g., v1.0, v1.1, v2.0)
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      working-directory: ${{ github.workspace }}
      
    - name: Build APK
      run: ./gradlew assembleRelease
      working-directory: ${{ github.workspace }}
      
    - name: Find APK
      run: find . -name "*.apk" -type f
      
    - name: Get version info
      id: version_info
      run: |
        VERSION_NAME=$(grep -A 2 "defaultConfig" app/build.gradle | grep "versionName" | tr -d ' "' | cut -d= -f2)
        VERSION_CODE=$(grep -A 2 "defaultConfig" app/build.gradle | grep "versionCode" | tr -d ' ' | cut -d: -f2)
        echo "version_name=$VERSION_NAME" >> $GITHUB_OUTPUT
        echo "version_code=$VERSION_CODE" >> $GITHUB_OUTPUT
        echo "build_name=TvShows V$VERSION_NAME($VERSION_CODE)" >> $GITHUB_OUTPUT
        echo "apk_filename=TvShows-V$VERSION_NAME($VERSION_CODE).apk" >> $GITHUB_OUTPUT
        
    - name: Rename APK
      id: rename_apk
      run: |
        ORIGINAL_APK=$(find app/build/outputs/apk/release -name "*.apk" | head -1)
        NEW_APK_NAME="${{ steps.version_info.outputs.apk_filename }}"
        NEW_APK_PATH="app/build/outputs/apk/release/$NEW_APK_NAME"
        cp "$ORIGINAL_APK" "$NEW_APK_PATH"
        echo "apk_path=$NEW_APK_PATH" >> $GITHUB_OUTPUT
        echo "apk_size=$(du -h "$NEW_APK_PATH" | cut -f1)" >> $GITHUB_OUTPUT
        echo "apk_name=$NEW_APK_NAME" >> $GITHUB_OUTPUT
        
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ steps.version_info.outputs.apk_filename }}
        path: ${{ steps.rename_apk.outputs.apk_path }}
        retention-days: 30
        
    - name: Send APK to Telegram
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        document: ${{ steps.rename_apk.outputs.apk_path }}
        caption: |
          🚀 *${{ steps.version_info.outputs.build_name }}*
          
          📦 Size: ${{ steps.rename_apk.outputs.apk_size }}
          
          🔗 [View on GitHub](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
        format: markdown
        parse_mode: Markdown

